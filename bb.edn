{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([qualcomm-snapdragon-spaces.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'qualcomm-snapdragon-spaces.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
